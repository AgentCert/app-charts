{{- if .Values.sockShop.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: carts-db
  labels:
    name: carts-db
  namespace: {{ include "sock-shop-litmus.sockShopNamespace" . }}
spec:
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    name: carts-db
{{- end }}
