{{- if .Values.sockShop.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: orders
  labels:
    name: orders
  namespace: {{ include "sock-shop-litmus.sockShopNamespace" . }}
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: orders
{{- end }}
