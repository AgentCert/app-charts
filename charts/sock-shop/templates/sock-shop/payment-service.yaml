{{- if .Values.sockShop.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    name: payment
  namespace: {{ include "sock-shop-litmus.sockShopNamespace" . }}
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: payment
{{- end }}
